# The static Vercel build reads this value when you replace the default in a
# preview artifact. At runtime, window.__FABOPS_API_BASE__ can override it.
FABOPS_API_BASE=https://semiconductor-fab-operations-api.onrender.com

